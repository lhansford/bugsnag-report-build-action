on: [push]

jobs:
  bugsnagReportBuild:
    runs-on: ubuntu-latest
    name: Bugsnag - Report Build
    steps:
    - name: report
      uses: actions/bugsnag-report-build-action@1.0.0
      with:
        apiKey: 12345
        failCiIfError: true
